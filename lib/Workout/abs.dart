import 'package:flutter/material.dart';

class AbsWorkout extends StatelessWidget {
  const AbsWorkout({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Abs')));
  }
}
