import 'package:flutter/material.dart';

class  ShouldersWorkout extends StatelessWidget {
  const ShouldersWorkout({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Shoulder')));
  }
}
