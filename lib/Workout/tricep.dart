import 'package:flutter/material.dart';

class TricepsWorkout extends StatelessWidget {
  const TricepsWorkout({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Triceps')));
  }
}
