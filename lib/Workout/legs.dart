import 'package:flutter/material.dart';

class LegsWorkout extends StatelessWidget {
  const LegsWorkout({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Legs')));
  }
}
