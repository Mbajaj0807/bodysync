import 'package:flutter/material.dart';

class ChestWorkout extends StatelessWidget {
  const ChestWorkout({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Chest')));
  }
}
